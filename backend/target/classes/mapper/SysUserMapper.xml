<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.mapper.SysUserMapper">

    <resultMap id="UserVOResultMap" type="com.school.vo.UserVO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="role_id" property="roleId"/>
        <result column="role_name" property="roleName"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectUserPage" resultMap="UserVOResultMap">
        SELECT u.id, u.username, u.real_name, u.phone, u.role_id, u.status, u.create_time,
               r.role_name
        FROM sys_user u
        LEFT JOIN sys_role r ON u.role_id = r.id
        WHERE u.deleted = 0
        <if test="username != null and username != ''">
            AND u.username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="realName != null and realName != ''">
            AND u.real_name LIKE CONCAT('%', #{realName}, '%')
        </if>
        <if test="roleId != null">
            AND u.role_id = #{roleId}
        </if>
        ORDER BY u.create_time DESC
    </select>

</mapper>
